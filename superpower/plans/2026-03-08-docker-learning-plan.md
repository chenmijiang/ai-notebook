# Docker 7 篇系列文章实施计划

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 创建 7 篇系统递进的 Docker 中文技术指南，覆盖从基础概念到 Docker Compose 多容器编排

**Architecture:** 按概念分层递进，每篇独立成文。前端场景贯穿全系列。遵循 tech-docs-guide 写作规范，与 ESLint/Prettier 系列风格一致

**Tech Stack:** Markdown、Docker、Docker Compose

---

## 一、文档命名规范

遵循「主题 + 完全指南」格式：

```
docs/docker-1-fundamentals.md  → # Docker 基础完全指南
docs/docker-2-images.md        → # Docker 镜像与仓库完全指南
docs/docker-3-containers.md    → # Docker 容器管理完全指南
docs/docker-4-dockerfile.md    → # Dockerfile 与镜像构建完全指南
docs/docker-5-networking.md    → # Docker 网络完全指南
docs/docker-6-storage.md       → # Docker 数据管理完全指南
docs/docker-7-compose.md       → # Docker Compose 完全指南
```

## 二、文档结构规范

每篇必须包含：

```markdown
# 标题

开头段落：一句话说明本篇讲什么。

> 本篇是 Docker 系列（共 7 篇）的第 N 篇。前置阅读：[第 N-1 篇标题](文件名)。下一篇：[第 N+1 篇标题](文件名)。

## 1. 第一个主题
### 1.1 子主题
...
## N. 总结
### N.1 核心要点
### N.2 速查表
## 参考资源
```

**禁止使用**：

- 目录（TOC）
- 章节分隔线 `---`

## 三、内容格式规范

参考 `.claude/skills/tech-docs-guide/SKILL.md`，核心要点：

| 要求     | 说明                                           |
| -------- | ---------------------------------------------- |
| 语言     | 简体中文                                       |
| 章节编号 | `## 1.` → `### 1.1` → `#### 1.1.1`             |
| 代码块   | 标注语言类型、中文注释、`// ✅` / `// ❌` 标记 |
| 表格     | 大量使用，用于对比、参数说明、速查             |
| 图解     | 简单用 ASCII，复杂用 Mermaid                   |
| 提示     | `> **注意**：` / `> **提示**：`                |

## 四、共享示例项目约定

全系列涉及前端场景的示例，统一使用以下约定：

| 约定项       | 统一值                                                                 |
| ------------ | ---------------------------------------------------------------------- |
| 前端框架     | React（Vite 构建）                                                     |
| API 服务     | Node.js + Express                                                      |
| 数据库       | PostgreSQL                                                             |
| 缓存         | Redis                                                                  |
| Web 服务器   | Nginx                                                                  |
| 服务名       | `web`（Nginx）、`api`（Node.js）、`db`（PostgreSQL）、`redis`（Redis） |
| 前端端口     | 宿主机 3000 → 容器 80                                                  |
| API 端口     | 宿主机 8080 → 容器 3000                                                |
| 数据库端口   | 宿主机 5432 → 容器 5432                                                |
| Redis 端口   | 宿主机 6379 → 容器 6379                                                |
| 项目目录     | `my-app/`（前端）、`my-api/`（API）                                    |
| 环境变量风格 | `UPPER_SNAKE_CASE`，如 `DATABASE_URL`、`REDIS_URL`、`NODE_ENV`         |

> **注意**：各篇示例按需使用其中一部分，不要求每篇都出现全部服务。第 7 篇 Compose 综合实战将全部串联。

## 五、篇幅控制规则

| 规则         | 说明                                              |
| ------------ | ------------------------------------------------- |
| 聚焦核心问题 | 每篇围绕一个核心问题展开，不发散                  |
| 不重复展开   | 前文已讲过的概念，用"回扣 + 链接"处理，不重新解释 |
| 表格替代叙述 | 能用表格、速查表说明的，不写长段文字              |
| 代码简洁     | 示例只包含说明问题的最少代码，不写完整项目        |
| 参考目标     | 每篇 400～800 行（与 ESLint/Prettier 系列一致）   |

## 六、每篇文档详细规范

### Task 1：docker-1-fundamentals.md

**标题**：`# Docker 基础完全指南`

**核心问题**：Docker 是什么，为什么需要它

**结构**：

```markdown
## 1. 概述
### 1.1 Docker 是什么
### 1.2 Docker 解决什么问题

## 2. 容器 vs 虚拟机
### 2.1 架构对比
### 2.2 性能差异
### 2.3 适用场景

## 3. Docker 架构
### 3.1 Client-Server 模型
### 3.2 Docker Daemon
### 3.3 Docker CLI
### 3.4 Registry

## 4. 核心概念
### 4.1 镜像（Image）
### 4.2 容器（Container）
### 4.3 仓库（Registry）
### 4.4 三者关系

## 5. 底层原理入门
### 5.1 Namespace（隔离）
### 5.2 Cgroup（资源限制）
### 5.3 UnionFS（分层文件系统）

## 6. 安装与验证
### 6.1 macOS 安装 Docker Desktop
### 6.2 验证安装

## 7. 第一个容器
### 7.1 docker run hello-world
### 7.2 背后发生了什么

## 8. 总结
### 8.1 核心要点
### 8.2 速查表

## 参考资源
```

**内容要点**：

- 容器 vs 虚拟机用架构图（ASCII 或 Mermaid）直观对比
- Docker 架构用流程图展示 Client → Daemon → Registry 交互
- 底层原理只建立心智模型，不做内核级展开，用类比帮助理解（如 Namespace ≈ 独立房间，Cgroup ≈ 房间电表）
- `docker run hello-world` 拆解为分步流程图，展示 pull → create → start → output → exit 全过程
- 镜像加速等网络环境配置不放主线，如需提及以 `> **提示**：` 形式放在安装章节末尾

**执行步骤**：

1. 创建文件 `docs/docker-1-fundamentals.md`，按上述结构编写完整内容
2. 通过 web fetch 验证 Docker 官方文档中的关键概念准确性
3. 自查：章节编号、代码注释、表格、提示格式是否符合规范
4. 运行 `npm run format` 格式化

---

### Task 2：docker-2-images.md

**标题**：`# Docker 镜像与仓库完全指南`

**核心问题**：镜像是什么，怎么选，怎么管

**结构**：

```markdown
## 1. 镜像基础
### 1.1 镜像是什么
### 1.2 只读分层结构
### 1.3 镜像 vs 容器

## 2. 镜像操作
### 2.1 拉取镜像（pull）
### 2.2 查看镜像（images / inspect / history）
### 2.3 标记镜像（tag）
### 2.4 推送镜像（push）
### 2.5 删除镜像（rmi）

## 3. Registry 与 Docker Hub
### 3.1 Registry 是什么
### 3.2 Docker Hub 使用
### 3.3 官方镜像 vs 社区镜像
### 3.4 镜像搜索与筛选

## 4. 标签策略
### 4.1 latest 的陷阱
### 4.2 语义化标签
### 4.3 锁定版本的最佳实践

## 5. 镜像分层深入
### 5.1 Layer 复用机制
### 5.2 docker history 解读
### 5.3 镜像大小分析

## 6. 镜像安全
### 6.1 来源可信度
### 6.2 漏洞扫描
### 6.3 前端常用基础镜像选择

## 7. 总结
### 7.1 核心要点
### 7.2 速查表

## 参考资源
```

**内容要点**：

- 分层结构用 ASCII 图示展示 layer 堆叠
- 每个 docker 命令都配完整示例和输出
- latest 陷阱用 ✅/❌ 对比展示
- 前端场景：node / nginx / alpine 基础镜像的选择对比表

**执行步骤**：

1. 创建文件 `docs/docker-2-images.md`，按上述结构编写完整内容
2. 通过 web fetch 验证镜像相关命令的最新语法
3. 自查规范合规性
4. 运行 `npm run format` 格式化

**阶段一完成检查点**：Task 1 + Task 2 完成后，检查基础术语是否统一（镜像/容器/仓库的用词、命令格式等）

---

### Task 3：docker-3-containers.md

**标题**：`# Docker 容器管理完全指南`

**核心问题**：容器怎么跑，怎么查，怎么控

**结构**：

```markdown
## 1. 容器生命周期
### 1.1 生命周期状态
### 1.2 状态转换图

## 2. 核心操作
### 2.1 创建与启动（run / create / start）
### 2.2 查看容器（ps / inspect）
### 2.3 停止与删除（stop / kill / rm）
### 2.4 暂停与恢复（pause / unpause）
### 2.5 重启（restart）

## 3. 观察与排障
### 3.1 查看日志（logs）
### 3.2 进入容器（exec / attach）
### 3.3 资源监控（stats / top）
### 3.4 详细信息（inspect）

## 4. 退出码
### 4.1 常见退出码含义
### 4.2 退出码排查流程

## 5. 环境变量与配置
### 5.1 -e 传递环境变量
### 5.2 --env-file 批量加载
### 5.3 配置注入模式

## 6. 资源限制
### 6.1 CPU 限制
### 6.2 内存限制
### 6.3 OOM 处理

## 7. 重启策略
### 7.1 四种策略对比
### 7.2 选择建议

## 8. 健康检查
### 8.1 健康状态是什么
### 8.2 健康状态查看与排障
### 8.3 docker run 中的健康检查参数

## 9. 常见故障排查
### 9.1 容器启动失败
### 9.2 容器意外退出
### 9.3 排查路径速查

## 10. 总结
### 10.1 核心要点
### 10.2 速查表

## 参考资源
```

**内容要点**：

- 生命周期状态转换用 Mermaid 状态图
- 退出码表格：码值 → 含义 → 常见原因 → 排查方法
- 重启策略四种模式用表格对比，附选择决策流程
- 故障排查提供"排查路径"流程图，不只是命令罗列
- 强化 inspect / logs / exec 的实战用法，这是本篇核心价值
- 健康检查重点讲"运行时观察"：健康状态是什么、怎么看、怎么用于排障；HEALTHCHECK 指令的声明方式留给第 4 篇 Dockerfile

**执行步骤**：

1. 创建文件 `docs/docker-3-containers.md`，按上述结构编写完整内容
2. 通过 web fetch 验证退出码、资源限制等参数的准确性
3. 自查规范合规性
4. 运行 `npm run format` 格式化

---

### Task 4：docker-4-dockerfile.md

**标题**：`# Dockerfile 与镜像构建完全指南`

**核心问题**：镜像怎么造，怎么优化（读者已理解容器运行机制）

**结构**：

```markdown
## 1. Dockerfile 基础
### 1.1 Dockerfile 是什么
### 1.2 第一个 Dockerfile

## 2. 指令详解
### 2.1 FROM
### 2.2 RUN
### 2.3 COPY 与 ADD
### 2.4 WORKDIR
### 2.5 ENV 与 ARG
### 2.6 EXPOSE
### 2.7 CMD 与 ENTRYPOINT
### 2.8 HEALTHCHECK

## 3. 指令对比
### 3.1 COPY vs ADD
### 3.2 CMD vs ENTRYPOINT
### 3.3 ARG vs ENV

## 4. 构建上下文
### 4.1 什么是构建上下文
### 4.2 .dockerignore 配置

## 5. 构建缓存
### 5.1 缓存工作原理
### 5.2 缓存失效规则
### 5.3 优化构建顺序

## 6. 多阶段构建
### 6.1 为什么需要多阶段构建
### 6.2 多阶段构建语法
### 6.3 前端项目多阶段构建

## 7. 前端项目实战
### 7.1 React SPA Dockerfile
### 7.2 Nginx 配置
### 7.3 完整示例

## 8. 构建优化
### 8.1 减小镜像体积
### 8.2 安全最佳实践
### 8.3 BuildKit 简介

## 9. 总结
### 9.1 核心要点
### 9.2 速查表

## 参考资源
```

**内容要点**：

- 每个指令配简洁示例 + 注意事项
- HEALTHCHECK 在此讲声明语法（第 3 篇已讲运行时如何观察健康状态，此处回扣链接）
- 三组指令对比（COPY/ADD、CMD/ENTRYPOINT、ARG/ENV）用表格 + 示例双重说明
- 构建缓存用"好的顺序 ✅ vs 坏的顺序 ❌"对比
- 多阶段构建重点展示前端场景：npm build → Nginx 服务
- 前端实战使用共享约定中的 React + Nginx，提供完整 Dockerfile + nginx.conf + .dockerignore
- BuildKit 作为"构建优化"的扩展补充，介绍 `--mount=type=cache` 等现代特性，标注为进阶内容

**执行步骤**：

1. 创建文件 `docs/docker-4-dockerfile.md`，按上述结构编写完整内容
2. 通过 web fetch 验证 Dockerfile 指令的最新行为和最佳实践
3. 自查规范合规性
4. 运行 `npm run format` 格式化

**阶段二完成检查点**：Task 3 + Task 4 完成后，检查第 2～4 篇边界是否清晰（第 2 篇"选与分发"、第 3 篇"运行与排障"、第 4 篇"构建与优化"），确认 HEALTHCHECK 在第 3/4 篇的分工无交叉

---

### Task 5：docker-5-networking.md

**标题**：`# Docker 网络完全指南`

**核心问题**：容器怎么通信，怎么暴露服务

**结构**：

```markdown
## 1. Docker 网络模型
### 1.1 容器网络基础
### 1.2 网络隔离原理

## 2. 端口映射
### 2.1 -p 语法详解
### 2.2 常见映射方式
### 2.3 为什么是 -p 宿主机:容器

## 3. Bridge 网络
### 3.1 默认 bridge 网络
### 3.2 默认 bridge 的局限
### 3.3 自定义 bridge 网络

## 4. 容器 DNS
### 4.1 自动 DNS 解析
### 4.2 为什么容器名可以互相访问
### 4.3 DNS 与默认 bridge 的区别

## 5. 容器间通信
### 5.1 同网络通信
### 5.2 跨网络通信
### 5.3 网络管理命令

## 6. 常见困惑解答
### 6.1 为什么容器里 localhost 不对
### 6.2 容器如何访问宿主机服务
### 6.3 多容器端口冲突处理

## 7. 前端场景
### 7.1 Nginx 反向代理到 API 容器
### 7.2 前后端联调网络配置

## 8. 扩展了解
### 8.1 host 网络模式
### 8.2 none 网络模式
### 8.3 overlay 网络模式简介

## 9. 总结
### 9.1 核心要点
### 9.2 速查表

## 参考资源
```

**内容要点**：

- 网络模型用 ASCII 图展示容器 → bridge → 宿主机 → 外部的数据流
- 端口映射多种写法用表格对比，使用共享约定中的端口号
- 默认 bridge vs 自定义 bridge 差异用表格 + 示例说明
- "常见困惑"章节直接回答高频问题，配命令验证
- 前端场景给出完整 Nginx 反向代理配置示例，使用共享约定中的服务名（web → api）
- overlay 等进阶内容标注"了解即可"，不做深入展开

**执行步骤**：

1. 创建文件 `docs/docker-5-networking.md`，按上述结构编写完整内容
2. 通过 web fetch 验证网络相关行为（特别是 host.docker.internal 等 macOS 特性）
3. 自查规范合规性
4. 运行 `npm run format` 格式化

---

### Task 6：docker-6-storage.md

**标题**：`# Docker 数据管理完全指南`

**核心问题**：数据怎么持久化，该选哪种方式

**结构**：

```markdown
## 1. 容器数据问题
### 1.1 容器文件系统特性
### 1.2 为什么数据会丢失
### 1.3 数据持久化方案概览

## 2. 挂载语法
### 2.1 -v 与 --mount 对比
### 2.2 推荐用法

## 3. 三种挂载方式
### 3.1 Volume
### 3.2 Bind Mount
### 3.3 tmpfs
### 3.4 三者对比与选择

## 4. Volume 详解
### 4.1 创建与管理
### 4.2 命名 Volume vs 匿名 Volume
### 4.3 Volume 数据查看
### 4.4 Volume 清理

## 5. Bind Mount 详解
### 5.1 基本用法
### 5.2 权限问题
### 5.3 开发环境热重载

## 6. 场景决策指南
### 6.1 开发环境怎么选
### 6.2 生产环境怎么选
### 6.3 决策流程图

## 7. 数据备份与恢复
### 7.1 Volume 备份
### 7.2 Volume 恢复
### 7.3 数据迁移

## 8. 前端项目常见坑
### 8.1 源码挂载与 HMR
### 8.2 node_modules 处理
### 8.3 构建产物持久化

## 9. 总结
### 9.1 核心要点
### 9.2 速查表

## 参考资源
```

**内容要点**：

- 新增 `-v` vs `--mount` 对比章节：语法差异、适用场景、官方推荐的 `--mount` 场景
- 数据丢失用"容器删除前后"的实验演示
- 三种方式对比用表格：适用场景、持久化、性能、可移植性
- "场景决策指南"是本篇核心，用决策流程图（Mermaid）引导选择
- node_modules 处理是前端 Docker 化的经典坑，详细说明匿名 Volume 技巧
- Bind Mount 权限问题给出 macOS 和 Linux 的差异说明

**执行步骤**：

1. 创建文件 `docs/docker-6-storage.md`，按上述结构编写完整内容
2. 通过 web fetch 验证存储相关的最新最佳实践
3. 自查规范合规性
4. 运行 `npm run format` 格式化

**阶段三完成检查点**：Task 5 + Task 6 完成后，检查网络/存储案例中的端口、服务名、目录路径是否与共享约定一致，以及是否与前文示例连贯

---

### Task 7：docker-7-compose.md

**标题**：`# Docker Compose 完全指南`

**核心问题**：怎么把前 6 篇组织成完整项目

**结构**：

```markdown
## 1. Compose 概述
### 1.1 从单容器到多容器
### 1.2 Compose 解决什么问题
### 1.3 安装与版本

## 2. 核心配置
### 2.1 compose.yml 基本结构
### 2.2 services
### 2.3 networks
### 2.4 volumes

## 3. 服务定义
### 3.1 image 与 build
### 3.2 ports
### 3.3 volumes
### 3.4 environment 与 env_file
### 3.5 depends_on 与服务就绪

## 4. 生命周期管理
### 4.1 up / down
### 4.2 logs
### 4.3 ps / exec
### 4.4 config（配置验证与展开）
### 4.5 常用操作速查

## 5. 环境管理
### 5.1 .env 文件
### 5.2 多环境配置
### 5.3 变量优先级

## 6. 综合实战
### 6.1 项目架构
### 6.2 前端服务（Nginx）
### 6.3 API 服务（Node.js）
### 6.4 数据库服务（PostgreSQL）
### 6.5 缓存服务（Redis）
### 6.6 完整 compose.yml
### 6.7 启动与验证

## 7. 总结
### 7.1 核心要点
### 7.2 速查表

## 参考资源
```

**内容要点**：

- 开篇回扣前 6 篇：之前手动管理的容器、网络、存储，Compose 用一个文件搞定
- `depends_on` 重点说明"依赖 ≠ 就绪"：depends_on 只控制启动顺序，不保证服务已准备好接受连接；配合 healthcheck 的 `condition: service_healthy` 才能真正等待就绪
- `docker compose config` 作为调试利器：展开变量替换、验证配置语法、排查 .env 问题
- 综合实战使用共享约定中的全部服务，提供完整可运行的项目结构：
  - compose.yml + 各服务 Dockerfile + nginx.conf + .env
  - 覆盖网络（服务间通信）、存储（数据库持久化）、环境变量等前文知识
- 突出 Compose 作为"胶水层"的角色，不重复讲已有知识，用"回扣 + 链接"方式引用前文

**执行步骤**：

1. 创建文件 `docs/docker-7-compose.md`，按上述结构编写完整内容
2. 通过 web fetch 验证 Compose V2 的最新配置语法
3. 自查规范合规性
4. 运行 `npm run format` 格式化

**阶段四完成检查点**：Task 7 完成后，检查 Compose 综合实战是否真正串联前 6 篇知识，是否承担了"整合收尾"角色而非独立展开新概念

---

### Task 8：全系列审校与发布接入

**核心问题**：7 篇文章作为一个系列是否一致、完整、可用

**执行步骤**：

1. **术语统一检查**：确认 7 篇中以下术语用词一致
   - 镜像/容器/仓库/构建/编排等核心术语
   - 命令格式（`docker run` vs `docker container run`，选择一种风格统一）
   - 英文术语的中文对照（如 Volume 是否始终保留英文）

2. **前后篇章衔接检查**：
   - 每篇开头的"系列位置说明"是否准确
   - 回扣链接是否指向正确的文件和章节
   - 是否有概念在多篇中重复展开（应收敛为"链接 + 一句话回扣"）

3. **共享示例一致性检查**：
   - 服务名、端口、目录结构是否与共享约定一致
   - 第 4 篇 Dockerfile 实战、第 5 篇 Nginx 配置、第 7 篇综合实战是否衔接

4. **删除不必要的重复内容**

5. **更新 `docs/index.md`**：将 Docker 系列 7 篇接入索引页

6. **运行 `npm run format` 全量格式化**

7. **最终 `npm run format:check` 验证**

---

## 七、版本控制策略

提交时机不绑定在每个 Task 内，而是按以下策略灵活决定：

| 策略           | 说明                                                                                             |
| -------------- | ------------------------------------------------------------------------------------------------ |
| 默认           | 每篇完成并通过自查后，暂存并提交                                                                 |
| 阶段检查点     | 阶段完成检查点发现问题并修正后，可合并为一次提交                                                 |
| Task 8         | 全系列审校完成后单独提交                                                                         |
| commit message | 格式：`docs: add Docker <主题> guide`，Task 8 用 `docs: finalize Docker series and update index` |

## 八、执行检查清单

创建每篇文档时必须检查：

- [ ] 使用简体中文
- [ ] 标题符合「主题 + 完全指南」格式
- [ ] 包含系列位置说明（前置阅读 + 下一篇）
- [ ] 章节编号正确（1. → 1.1 → 1.1.1）
- [ ] 无目录、无 `---` 分隔线
- [ ] 代码块标注语言类型、有中文注释
- [ ] 使用 `// ✅` 和 `// ❌` 标记好坏实践
- [ ] 大量使用表格（对比、参数、速查）
- [ ] 简单图用 ASCII，复杂图用 Mermaid
- [ ] 有总结章节（核心要点 + 速查表）
- [ ] 有参考资源章节
- [ ] 示例使用共享约定中的服务名、端口、目录
- [ ] 通过 web fetch 验证内容准确性
- [ ] 篇幅控制在 400～800 行
- [ ] 运行 `npm run format` 格式化

## 九、实施顺序

```
阶段一：入门
├── Task 1: docker-1-fundamentals.md（基础概念）
├── Task 2: docker-2-images.md（镜像与仓库）
└── 检查点：基础术语统一

阶段二：核心
├── Task 3: docker-3-containers.md（容器管理）
├── Task 4: docker-4-dockerfile.md（Dockerfile）
└── 检查点：第 2～4 篇边界清晰、HEALTHCHECK 分工无交叉

阶段三：进阶
├── Task 5: docker-5-networking.md（网络）
├── Task 6: docker-6-storage.md（数据管理）
└── 检查点：案例与共享约定一致

阶段四：综合
├── Task 7: docker-7-compose.md（Compose 编排）
├── 检查点：综合实战串联前 6 篇
└── Task 8: 全系列审校 + index.md 更新
```
